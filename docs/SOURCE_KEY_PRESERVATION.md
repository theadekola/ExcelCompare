# Source key preservation

Version 2.0.1 stores separate exact source keys for Workbook 1 and Workbook 2.
Normalised keys are used only internally for matching. The Results page and Excel, CSV and PDF reports retain prefixes such as `LN` or `LNS`, leading zeroes, original letter case and source spacing.
